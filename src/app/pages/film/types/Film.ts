export type Film = {
  id: string

  cast: string[],
  contry: string
  director: string
  durationMinutes: number,
  genre: string[],
  language: string
  posterURL: string
  rating: number,
  releaseYear: number,
  synopsis: string
  title: string
  trailerURL: string
}
